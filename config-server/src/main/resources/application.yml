server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jeffagostinho/microservices-java-spring-boot-config-server
          search-paths: "greeting-service*"